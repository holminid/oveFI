# Tagging system notes

These tags are not decorative. They are shared semantic handles.

The same handle can connect a work entry, a research note, and a Composer resolver page.

Example handles: `ostinato`, `cadence`, `texture`, `voice`, `collective behavior`, `biosignal`, `macro form`.
