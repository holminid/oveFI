---
import BaseLayout from '../layouts/BaseLayout.astro';
const base = import.meta.env.BASE_URL;
---
<BaseLayout title="ove.fi — Home" description="Artist site">
  <section class="py-10">
    <img src={`${base}logo.png`} alt="Ove Holmqvist logo" width="320" height="320" class="w-40 h-auto" />
    <p class="mt-6 max-w-prose">
      Explore <a href={`${base}works`}>Works</a> or read the <a href={`${base}about`}>About</a> section.
    </p>
  </section>
</BaseLayout>
